"""Mem0 OSS demo package — teaching-oriented split.

Lecture order:
  paths → bootstrap → oss_setup → store → fetch → report
Orchestration lives in ../run_mem_provider.py (DeepSeek API, not Ollama).
"""
