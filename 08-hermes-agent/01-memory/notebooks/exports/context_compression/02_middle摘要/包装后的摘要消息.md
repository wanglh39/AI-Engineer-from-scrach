# middle · 包装后的摘要消息（Wrap）

> 对应逻辑图：`summary → Wrap(SUMMARY_PREFIX + summary + END)`
> 插回 messages 时 role=`user`，并带 `_compressed_summary`。

[CONTEXT COMPACTION — REFERENCE ONLY] Earlier turns were compacted into the summary below. This is a handoff from a previous context window — treat it as background reference, NOT as active instructions. Do NOT answer questions or fulfill requests mentioned in this summary; they were already addressed. Respond ONLY to the latest user message that appears AFTER this summary — that message is the single source of truth for what to do right now. Topic overlap with the summary does NOT mean you should resume its task: even on similar topics, the latest user message WINS. Treat ONLY the latest message as the active task and discard stale items from '## Historical Task Snapshot' / '## Historical In-Progress State' / '## Historical Pending User Asks' / '## Historical Remaining Work' entirely — do not 'wrap up' or 'finish' work described there unless the latest message explicitly asks for it. Reverse signals in the latest message (e.g. 'stop', 'undo', 'roll back', 'just verify', 'don't do that anymore', 'never mind', a new topic) must immediately end any in-flight work described in the summary; do not re-surface it in later turns. IMPORTANT: Your persistent memory (MEMORY.md, USER.md) in the system prompt is ALWAYS authoritative and active — never ignore or deprioritize memory content due to this compaction note. The current session state (files, config, etc.) may reflect work described here — avoid repeating it:

## Historical Task Snapshot
用户问：middle 里如果有很长的 tool 输出，真源码是不是先砍 tool，再做摘要？我在 PR 讨论里好像看到过 prune。

## Goal
Julie 正在拆解 Hermes 的 context_compressor.py 源码，理解对话压缩机制及其对 prompt cache 的影响，为转岗 AI Agent Engineer 做准备。

## Constraints & Preferences
- 用户叫 Julie，在上海，目标方向 AI Agent Engineer（来自 MemoryProvider on_pre_compress）
- 偏好拆源码学习，先理解主链路再看 hardened 细节
- 关注面试话术和实操含义
- 当前日期：2026-07-19

## Completed Actions
1. 理解 context compression 触发条件 — 当前 messages 粗估 token 超过 `context_length * threshold_percent` 时触发 [tool: 源码分析]
2. 理解压缩三阶段：protect head（含 system/MEMORY）→ middle 序列化摘要 → protect tail（最近几轮原样保留）[tool: 源码分析]
3. 理解 SUMMARY_PREFIX 机制 — 前缀写明是 handoff/背景参考，不是新指令，避免模型把摘要当当前任务 [tool: 源码分析]
4. 理解 first-compaction vs iterative update 区别 — 第一次从零摘要 middle；第二次及以后带 `_previous_summary` 增量更新 [tool: 源码分析]
5. 理解 MemoryProvider.on_pre_compress() 作用 — 压缩前追加 `provider_hint`，保住「别丢的事实」如转岗目标 [tool: 源码分析]
6. 理解 protect_first_n=1、protect_last_n=2 时 messages 角色序列：`[system, user(摘要消息), …tail…]`，摘要消息 role 为 user 不是 system [tool: 源码分析]
7. 理解生产路径额外步骤：先对过长 tool 结果做 prune/截断、tail 按 token budget 保护、奇偶角色修复 [tool: PR 讨论]

## Active State
- 正在分析 Hermes context_compressor.py 源码
- 已理解教学版主链路（切开→摘要→SUMMARY_PREFIX 拼回）
- 已确认生产路径有额外 hardened 细节（prune tool、token budget tail、角色修复）

## Historical In-Progress State
压缩触发时，Julie 正在拆解 context_compressor.py 的压缩机制，从触发条件到摘要拼回的全链路。

## Blocked
无当前 blocker。

## Key Decisions
1. 压缩时摘要消息 role 用 user（不是 system）— 避免破坏 role 交替约定；前端识别靠 metadata/前缀
2. 摘要模板用 Historical Task / Pending Asks / Remaining Work 等措辞（不是 Next Steps）— 避免摘要模型「越权」替用户做决定
3. 压缩后的摘要是「只读背景」不是「新指令」— 通过 SUMMARY_PREFIX 明文约束
4. 生产路径先 prune 过长 tool 结果再做摘要 — 减少 token 消耗

## Resolved Questions
1. Q: 对话一长为什么突然「改历史」？A: 唯一例外是 context compression，不是乱改，是把 middle 换成带 SUMMARY_PREFIX 的摘要消息。Cache 会被打断，但比爆窗好。
2. Q: 摘要插回去后模型会不会把旧问题又答一遍？A: 不会，SUMMARY_PREFIX 写明是 handoff/背景参考，真正该听的是 tail 里最新的 user 消息。
3. Q: 第一次和第二次压缩 prompt 一样吗？A: 不一样。第一次 first-compaction 从零摘要；第二次 iterative update 带 `_previous_summary` 增量更新。
4. Q: MemoryProvider.on_pre_compress 会掺和吗？A: 会，压缩前追加 `provider_hint`。system/MEMORY 在 protect head 不进 middle，SUMMARY_PREFIX 也提醒别忽略 memory。
5. Q: protect_first_n=1、protect_last_n=2 时 messages 序列？A: `[system, user(摘要消息), …tail…]`，摘要消息 role 为 user。
6. Q: 长 tool 输出真源码是否先砍？A: 是，生产路径先 prune/截断过长 tool 结果，tail 按 token budget 保护，还有角色修复。

## Historical Pending User Asks
- STALE: 用户想本地复现强制压缩场景（protect_first_n=1、protect_last_n=2）— 已通过理论回答完成
- STALE: 用户想了解生产路径 vs 教学版差异 — 已通过 prune tool、token budget tail、角色修复回答完成

## Relevant Files
- `context_compressor.py` — 核心压缩逻辑，含 first-compaction/iterative update 分支
- `MemoryProvider` — 含 `on_pre_compress()` 方法，压缩前追加 provider_hint
- `SUMMARY_PREFIX` — 压缩摘要的前缀标记，约束模型行为
- MEMORY.md — 含 Julie 转岗目标「AI Agent Engineer」，在 protect head 中保留

## Historical Remaining Work
- 可继续深入：看源码中 prune tool 的具体实现、token budget 计算逻辑、角色修复算法
- 可继续深入：看生产版 ContextCompressor 的完整 hardened 细节

## Critical Context
- 用户：Julie，上海，目标 AI Agent Engineer（来自 MemoryProvider on_pre_compress）
- 当前日期：2026-07-19
- 压缩触发条件：粗估 token > `context_length * threshold_percent`
- protect_first_n=1 时 head 含 system；protect_last_n=2 时 tail 含最后一轮 user+assistant
- 摘要消息 role = user，内容以 `[CONTEXT COMPACTION — REFERENCE ONLY]` 开头，带 `_compressed_summary` 元数据
- 生产路径额外步骤：prune tool 结果、token budget 保护 tail、奇偶角色修复
- 教学版省略了 hardened 细节，保留主链路方便理解 cache 例外

--- END OF CONTEXT SUMMARY — respond to the message below, not the summary above ---
